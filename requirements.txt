# Base runtime — intentionally torch-free so the container builds fast and
# `docker compose up` always works (motion backend). YOLO is an opt-in extra.
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
PyYAML==6.0.2
numpy==2.2.1
opencv-python-headless==4.10.0.84
